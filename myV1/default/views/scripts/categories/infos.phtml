<?php 
   $categorie     = $this->categorie;
   
   $linkEdit      = $this->url(array("controller" => "categories", "action" => "edit" , "id" => $categorie->catid) , "default" ) ;
   $linkCancel    = $this->url(array("controller" => "categories", "action" => "list"), "default" ) ;
     
   $buttonbar     = $this->Btnbar(array("id" => "MyBtnBar") , $this->jqueryHandler());  
   $buttonEdit    = $buttonbar->button(" Mettre à jour les informations", "edit")->setPriority("primary");
   $buttonCancel  = $buttonbar->button(" Revenir sur la liste", "ban");
   
   $buttonbar->add( $buttonEdit   , "editButton");
   $buttonbar->add( $buttonCancel , "cancelButton");
   
   $buttonEdit->onClick("  event.preventDefault(); document.location.href= '".$linkEdit."' ;");
   $buttonCancel->onClick("event.preventDefault(); document.location.href= '".$linkCancel."' ;");
?>
  
 <div class="container pagecontainer col-md-12" style="text-align:left;">
    <ul class="list-group">
     <li class="list-group-item">
       <div class="row"> 
            <div class="col-md-4"> <label class="label" for="libelle"> <b>  Désignation du type de produits: </b> </label></div> 
            <div class="col-md-8"> <?php echo $this->escape($categorie->libelle);  ?></div>
       </div>
     </li>             
     <li class="list-group-item">
       <div class="row"> 
            <div class="col-md-4"> <label class="label" for="description"> <b> Description :</b> </label></div> 
            <div class="col-md-8"> <?php echo $this->escape($categorie->description);  ?> </div>
       </div>
   </li>     
  </ul>
  </div>
    <?php 
          if(!$this->isAjax) {
          	 echo $buttonbar;
          } else {
          	 echo $this->jQuery();
          }
      ?>   