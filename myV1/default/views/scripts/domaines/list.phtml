<?php 
$domaines       = $this->domaines;
$filters        = $this->filters;
$paginator      = $this->paginator;

$basePath       = VIEW_BASE_PATH;
$jq             = $this->jQuery();

$linkSearch     = $this->url(array("controller" => "domaines", "action"=> "list")  , "default");
$linkCreate     = $this->url(array("controller" => "domaines", "action"=> "create"), "default");
$linkEdit       = $this->url(array("controller" => "domaines", "action"=> "edit"  ), "default");
$linkDelete     = $this->url(array("controller" => "domaines", "action"=> "delete"), "default");
 
$jq->addOnload("
jQuery('.resetPage').click(function(){event.preventDefault();document.location.href='".$linkSearch."'; });
jQuery('#paginationBtnNext').click(function(  event ){
		event.preventDefault();
		if( jQuery(this).hasClass('disabled') ) {
		    return;
		}
		var formSelector   = jQuery('#domaineSearchForm') ;
		var keywordsText   = jQuery.trim(formSelector.find('#keywords').val());		
		var pageNum        = parseInt(   formSelector.find('#page').val());
		pageNum++;
		
		var searchData     = {};
		searchData.page    = pageNum;
		searchData.maxitems= parseInt(formSelector.find('#maxitems').val());						 
		searchData.libelle = keywordsText;
        var maincontent	   = jQuery('#maincontent');	
		SIRAH.loading.on( maincontent , 'Chargement des données...' );
		SIRAH.request.post(formSelector, '".$linkSearch."' , 
		                   {  dataType   :'html' ,
		                      async      : true,
		                      data       : searchData,
		                      beforeSend : function() { },
		                      error      : function(data){maincontent.html(data);SIRAH.loading.off(false);},
	                          success    : function(data){maincontent.html(data);SIRAH.loading.off(false);},
		                      update     : function(data){maincontent.html(data);SIRAH.loading.off(false);}		
		                  });
});
jQuery('#paginationBtnPrev').click(function(  event ){
		event.preventDefault();
		if( jQuery(this).hasClass('disabled') ) {
		    return;
		}
		var formSelector   = jQuery('#domaineSearchForm') ;
		var keywordsText   = jQuery.trim(formSelector.find('#keywords').val());		
		var pageNum        = parseInt(   formSelector.find('#page').val());
		pageNum--;
		
		var searchData     = {};
		searchData.page    = pageNum;
		searchData.maxitems= parseInt(formSelector.find('#maxitems').val());						 
		searchData.libelle = keywordsText;
        var maincontent	   = jQuery('#maincontent');	
		SIRAH.loading.on( maincontent , 'Chargement des données...' );
		SIRAH.request.post(formSelector, '".$linkSearch."' , 
		                   {  dataType   :'html' ,
		                      async      : true,
		                      data       : searchData,
		                      beforeSend : function() { },
		                      error      : function(data){maincontent.html(data);SIRAH.loading.off(false);},
	                          success    : function(data){maincontent.html(data);SIRAH.loading.off(false);},
		                      update     : function(data){maincontent.html(data);SIRAH.loading.off(false);}		
		                  });
});
jQuery('#searchDomaine').click(function( event ){
		event.preventDefault();
		var formSelector   = jQuery('#domaineSearchForm') ;
		var keywordsText   = jQuery.trim(formSelector.find('#keywords').val());		
		
		var searchData     = {};
		searchData.maxitems= parseInt(formSelector.find('#maxitems').val());						 
		searchData.libelle = keywordsText;

        var maincontent	   = jQuery('#maincontent');	
		SIRAH.loading.on( maincontent , 'Chargement des données...' );
		SIRAH.request.post(formSelector, '".$linkSearch."' , 
		                   {  dataType   :'html' ,
		                      async      : true,
		                      data       : searchData,
		                      beforeSend : function() { },
		                      error      : function(data){maincontent.html(data);SIRAH.loading.off(false);},
	                          success    : function(data){maincontent.html(data);SIRAH.loading.off(false);},
		                      update     : function(data){maincontent.html(data);SIRAH.loading.off(false);}		
		                  });						
});
jQuery('.paginationCountItem').click(function( event ){
		event.preventDefault();
		var paginationMaxitem  = parseInt(jQuery(this).attr('id').replace('paginationMaxitem-',''));	
		if( !paginationMaxitem ) {
		    return;
		}	
		var formSelector      = jQuery('#domaineSearchForm') ;
		formSelector.find('#maxitems').val( paginationMaxitem );
		var searchData        = {};
		searchData.maxitems   = parseInt(formSelector.find('#maxitems').val());						 
		searchData.libelle    = keywordsText;
        var maincontent	      = jQuery('#maincontent');	
		SIRAH.loading.on( maincontent , 'Chargement des données...' );
		SIRAH.request.post(formSelector, '".$linkSearch."' , 
		                   {  
		                      dataType   :'html' ,
		                      async      : true,
		                      data       : searchData,
		                      beforeSend : function() { },
		                      error      : function(data){maincontent.html(data);SIRAH.loading.off(false);},
	                          success    : function(data){maincontent.html(data);SIRAH.loading.off(false);},
		                      update     : function(data){maincontent.html(data);SIRAH.loading.off(false);}		
		                  });
		
});
jQuery('input:checkbox').uniform({checkboxClass:'checkbox-primary'});
	var getCheckedDomainesId = function() {
		var checkedItems = jQuery('#domainesListTable').find('input[type=\'checkbox\'][name!=\'checkall\']:checked');
        var checkedIds   = []; 
		if(checkedItems) {
		   checkedItems.each(function() {
             	var domaineid   = parseInt(jQuery(this).attr('id').replace('domaineids',''));
             	checkedIds.push( domaineid );
           });
		}
		return checkedIds;
  };
  jQuery('#domainesListTable').find('input[type=\'checkbox\']').click(function(e){
		 var checkedItems  = jQuery('#domainesListTable').find('input[type=\'checkbox\'][name!=\'checkall\']:checked');
		 if(jQuery(this).attr('id') == 'checkAll') {
		    if(!jQuery(this).is(':checked')) {
		       checknone(jQuery('#domainesListTable'));
		       if( checkedItems ) {
		           disableButtons(['editButton','deleteButton']);
		       }
		    } else {
		      checkall(jQuery('#domainesListTable'));
		      enableButtons(['editButton','deleteButton']);
		    }
		 } else {
		  if(jQuery(this).is(':checked')) {
		     enableButtons(['editButton','deleteButton']);
		  } else {
		      if( SIRAH.util.isEmpty(checkedItems)) {
		          disableButtons(['editButton','deleteButton']);
		       }
		  }
		}
   });
		");

$toolbar      = $this->supinaToolsbar(array() , $this->jqueryHandler());
$toolCreate   = $toolbar->tool("Enregistrer un secteur d'activités", "plus");
$toolEdit     = $toolbar->tool("Modifier" , "edit");
$toolDelete   = $toolbar->tool("Supprimer", "trash-o");

$toolbar->add( $toolCreate, "createButton");
$toolbar->add( $toolEdit  , "editButton");
$toolbar->add( $toolDelete, "deleteButton");

$toolCreate->onClick(sprintf("event.preventDefault(); document.location.href='%s';" , $linkCreate ));

$toolEdit->disable();
$toolEdit->onClick("
		             event.preventDefault();
		             var isDisabled   = jQuery(this).is('.disabled') || jQuery(this).is('.btn-disabled');
		             var checkedItem  = jQuery('#domainesListTable').find('input[type=\'checkbox\'][name=\'domaineids[]\']:checked').eq(0);
		             if( isDisabled || !checkedItem) {
		                 SIRAH.dialog.alert('Vous devrez selectionner une ligne avant d\'effectuer cette opération','Selectionnez une ligne');
		                 return;
                     }
		             var domaineid     = parseInt(checkedItem.attr('id').replace('domaineids',''));
		             var domainename   = jQuery('#name-'+domaineid).val();
		             var dialogEdit    = jQuery('<div></div>').attr('id','dialogEdit').addClass('modal-medium');
		
		             dialogEdit.dialog2({
		                                  title : 'Mettre à jour les informations du domaine `<u>'+domainename+'</u>`',
		                                  content : '".$linkEdit."/domaineid/'+domaineid,
		                                  autoOpen: false,
		                                  modalClass:'modal-wide',
		                                  removeOnClose: true,
		                                  closeOnEscape: false,
		                                  closeOnOverlayClick: true,
		                                  showCloseHandle:true,
		                                  initialLoadText: 'Chargement...'
                                       });
		            dialogEdit.dialog2('addButton','<span class=\'glyph-icon icon-edit\'></span> Sauvegarder',{primary:true,click :function(){var editForm=jQuery('#dialogEdit').find('form').eq(0);SIRAH.request.post(editForm,'".$linkEdit."/domaineid/'+domaineid);}});
		            dialogEdit.dialog2('addButton','<i class=\'glyph-icon icon-ban\'> </i> Annuler', {primary:false,click:function(){jQuery(this).dialog2('close');}});
		            dialogEdit.dialog2('open');");
$toolDelete->disable();
$toolDelete->onClick(" 
		               var isDisabled        = jQuery(this).is('.disabled') || jQuery(this).is('.btn-disabled');
		               var checkedDomaines = getCheckedDomainesId();
		               if( isDisabled || SIRAH.util.isEmpty( checkedDomaines )) {
		                   SIRAH.dialog.alert('Vous devrez selectionner au moins une ligne avant d\'effectuer cette opération','Selectionnez une ligne');
		                   return;
                       }
		               var dialogDelete = jQuery('<div> Voulez-vous vraiment supprimer les domaines d\'activités selectionnés ? </div>').attr('id','dialogDelete');             		 
             		   dialogDelete.dialog2({
             		                          title : 'Attention , confirmer cette action !!',
             		                          autoOpen: false,
             		                          removeOnClose: true,
             		                          closeOnEscape: false, 
                                              closeOnOverlayClick: true,
             		                          showCloseHandle:true,
             		                          initialLoadText: 'Chargement...' });        		
             		   dialogDelete.dialog2('addButton','<i class=\'glyph-icon icon-check\'> </i> Oui',{primary:true,click : 
             		                            function(){
             		                                        jQuery.ajax({
             		                                             url      :'".$linkDelete."',
             		                                             async    : false,
			                                                     dataType :'json',
             		                                             data     : {'domaineids[]': checkedDomaines},
             		                                             type     : 'GET' ,
             		                                             success  : function(data) {
             		                                                if(data.error !== undefined) {
             		                                                   SIRAH.error.raise(jQuery('#dialogDelete') , data.error);
             		                                                   return;
             		                                                } else if(data.success !== undefined) {
             		                                                   var checkedDomaines = getCheckedDomainesId();  	            		
             		                                                  SIRAH.error.raise(jQuery('#dialogDelete'), data.success , 'success');
             		                                                  setTimeout(function(){jQuery.each(checkedDomaines,function(key,domaineid){jQuery('#domaineRowId-'+domaineid).remove();});jQuery('#dialogDelete').dialog2('close');},2500);
             		                                               }
             		                                             }
             		                                           });     		
                                                           }}); 
             		      dialogDelete.dialog2('addButton','<i class=\'glyph-icon icon-ban\'></i>Annuler',{primary:false,click:function(){jQuery(this).dialog2('close');}});              		
             		      dialogDelete.dialog2('open'); 

		");

$this->toolsbar = $toolbar;

$pagination     = $this->supinaPagination(array(), "Jumping" , "#");

?>

<div id="domainesListWrapper"  class="row">
  <form id="domaineSearchForm" class="" method="post" role="form">
    <div  class="col-md-12">
       <div class="content-box">
           <div class="mail-header clearfix">               
                <div class="float-right col-md-4 pad0A">
                    <div class="input-group">
                        <input type="text" class="form-control" value="<?php echo $filters['libelle']; ?>" name="keywords" id="keywords" placeholder="Saisir des mots clés">
                        <div class="input-group-btn">
                            <button id="searchDomaine" type="button" class="btn btn-default" tabindex="-1"><i class="glyph-icon icon-search"></i></button>
                            <button  type="button" class="btn alert-danger resetPage" tabindex="-1"><i class="glyph-icon icon-remove"></i></button>
                        </div>
                    </div>
               </div>
           </div>                                                                  
           <div class="mail-toolbar clearfix"> 
              <div class="float-left" > 
                 <a href="#" title="Actualiser la page" class="btn btn-default mrg5R resetPage"><i class="glyph-icon font-size-11 icon-refresh"></i></a>
                            
              </div>
              <div class="float-right"> <?php  echo $pagination->render(); ?> </div>
           </div>
           <table id="domainesListTable"  class="table table-hover table-bordered table-stripped table-responsive sirah-ui-table scrollable text-center">
                <thead>
                     <tr> 
                          <th width="1%"> <input type="checkbox" name="checkall" id="checkAll" class="checkAll" /></th>
                          <th width="4%">  IDs                    </th> 
                          <th width="45%"> Domaines d'activités   </th> 
                          <th width="50%"> Description            </th> 
                    </tr>
                </thead>                
                <tbody>
                       <?php 
                              $output   = "";
                              if( count(   $domaines ) ) {
                              	  foreach( $domaines as $domaine ) {
                              	  	       $domaineid            = intval($domaine['domaineid']);
                              	  	       $domaineLibelle       = $domaine["libelle"];
                              	  	       $domaineDescription   = $domaine["description"];
                              	  	       $linkInfos            = $this->url(array("controller" => "domaines", "id" => $domaineid, "action" => "infos"), "default");

                              	  	       $output .= "<tr id=\"domaineRowId-".$domaineid."\">";
                              	  	                $output .= "<td> <input type=\"checkbox\" name=\"domaineids[]\" id=\"domaineids".$domaineid."\" class=\"domaineids\" /> </td>";
                              	  	                $output .= "<td> <a href=\"".$linkInfos."\"> ".$domaineid           ." </a> </td>";
                              	  	                $output .= "<td> <a href=\"".$linkInfos."\"> ".$domaineLibelle      ." </a> </td>";
                              	  	                $output .= "<td> <a href=\"".$linkInfos."\"> ".$domaineDescription  ." </a> </td>";
                              	  	        $output .= "</tr>";
                              	  	        
                              	  	        $output .= "<input type=\"hidden\"  name=\"name-".$domaineid."\" id=\"name-".$domaineid."\"   value=\"".$domaineLibelle."\"  />" ;
                              	  }                              	
                              }  else {
                              	$output .= "<tr id=\"domaineRowId-0\"> <td style=\"text-align:center;\" colspan=\"4\"> Aucun secteur d'activités n'a été retrouvé </td> </tr>";
                              }                              
                              echo $output;                      
                       ?>
                
                
                </tbody>           
           </table>
       </div>    
    </div>
    <input type="hidden" name="page"      id="page"     value="<?php echo $this->pageNum;  ?>"  /> 
    <input type="hidden" name="maxitems"  id="maxitems" value="<?php echo $this->pageSize; ?>"  />
  </form>
</div>
<?php 
     if( $this->isAjax ) {
     	 echo $this->jQuery();
	     echo $this->headStyle();
     }
?>   